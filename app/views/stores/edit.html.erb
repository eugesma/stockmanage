<%= render "header" %>

<div class="row">
  <div class="col-6 mx-auto">
    <div class="card">
      <div class="card-header bg-warning">        
        <h5 class="title">
          <%= fa_icon "edit" %> Editando tienda
        </h5>
      </div>
      <%= render 'form', store: @store %>
    </div>
  </div>
</div>