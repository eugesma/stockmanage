<%= render "header" %>

<div class="row">
  <div class="col-6 mx-auto">
    <div class="card">
      <div class="card-header bg-primary text-white">        
        <h5 class="title">
          <%= fa_icon "plus" %> Nueva tienda
        </h5>
      </div>
      <%= render 'form', store: @store %>
    </div>
  </div>
</div>